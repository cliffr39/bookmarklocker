<?xml version="1.0" encoding="utf-8"?>
<!-- themes.xml: Defines the visual themes for the Bookmark Locker application.
     It uses MaterialComponents for a modern Android look and feel.
     Both a light theme and a dark theme (for night mode) are defined.
     Updated in Step 6 to include chip_corner_radius dimension. -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Light mode. -->
    <style name="Theme.BookmarkLocker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/material_dynamic_primary40</item>
        <item name="colorPrimaryVariant">@color/material_dynamic_primary10</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/material_dynamic_secondary40</item>
        <item name="colorSecondaryVariant">@color/material_dynamic_secondary20</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/material_dynamic_neutral_variant95</item>
        <!-- Text input layout default style -->
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
        <!-- Button styles -->
        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button</item>
        <!-- Collection stroke color for light mode -->
        <item name="collectionStrokeColor">@color/collection_stroke_light</item>
    </style>

    <!-- Night mode theme. Inherits from the base theme. -->
    <style name="Theme.BookmarkLocker.Night" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Override colors for dark mode if needed, e.g., using dark variants -->
        <item name="colorPrimary">@color/material_dynamic_primary80</item>
        <item name="colorPrimaryVariant">@color/material_dynamic_primary90</item>
        <item name="colorOnPrimary">@color/material_dynamic_primary20</item>
        <item name="colorSecondary">@color/material_dynamic_secondary80</item>
        <item name="colorSecondaryVariant">@color/material_dynamic_secondary90</item>
        <item name="colorOnSecondary">@color/material_dynamic_secondary20</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowBackground">@color/material_dynamic_neutral10</item>
    </style>

    <!-- Define a shape appearance overlay for FAB to make it perfectly circular -->
    <style name="ShapeAppearanceOverlay.App.CornerSize50Percent" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <!-- Custom stroke color for collection items in light mode -->
    <color name="collection_stroke_light">@color/material_dynamic_neutral_variant80</color>

</resources>
